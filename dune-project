(lang dune 2.8)
; This file was generated by drom, using drom.toml

(cram enable)
(name yalo)
(generate_opam_files false)
(version 0.1.0)
(formatting (enabled_for ocaml reason))

(package
 (name yalo)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    (share_root yalo)
  )

 (depends
   (ocaml (>= 4.08.0))
   (dune-site ( >= 3.14.0 ))
   (yalo_driver (= :version))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_misc)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   yojson
   (yalo_config (= :version))
   re
   ppx_deriving_yojson
   (ez_file (and (>= 0.3.0) (< 1.0.0)))
   (ez_cmdliner (and (>= 0.2.0) (< 1.0.0)))
   (directories ( = 0.2 ))
   base-unix
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_lib)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_misc (= :version))
   (yalo_config (= :version))
   re
   (ez_file (and (>= 0.3.0) (< 1.0.0)))
   (ez_cmdliner (and (>= 0.2.0) (< 1.0.0)))
   (directories ( = 0.2 ))
   base-unix
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name ppx_yalo)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_plugin_ocaml (= :version))
   (yalo_lib (= :version))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_plugin_ocaml)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    (share_root yalo)
  )

 (depends
   (ocaml (>= 4.08.0))
   (dune-site ( >= 3.14.0 ))
   (yalo_lib (= :version))
   (ppxlib ( >= 0.35 ))
   ppx_optcomp
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_plugin_YALO)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_plugin_ocaml (= :version))
   (yalo_lib (= :version))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_plugin_FIND)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_plugin_ocaml (= :version))
   (yalo_lib (= :version))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_plugin_ZANUDA)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_plugin_ocaml (= :version))
   (yalo_lib (= :version))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_driver)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (yalo_lib (= :version))
   (ez_file (and (>= 0.3.0) (< 1.0.0)))
   (ez_cmdliner (and (>= 0.2.0) (< 1.0.0)))
   (directories ( = 0.2 ))
   base-unix
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_config)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   (ez_file (and (>= 0.3.0) (< 1.0.0)))
   camlp-streams
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(package
 (name yalo_tests)
 (synopsis "The yalo platform (Yet Another Linter for OCaml) is a plugin-based code scanner for OCaml projects and potentially other languages")
 (description "Yalo aims at providing a code scanner for OCaml projects. Because most organizations want to easily add their own linting rules, Yalo is plugin-based, it provides a simple to use interface to easily write such plugins. Projects can easily be configured to use Yalo, activate the warnings/errors that they want, and integrate the tool in Github Actions thanks to SARIF JSON output.\n")
 
  (sites
    
  )

 (depends
   (ocaml (>= 4.08.0))
   ppx_inline_test
   ppx_expect
   odoc
   ocamlformat
  )
 )

(using dune_site 0.1)


; add this in dune-project-trailer if you need it, as it is not used
; by the most recent versions of dune
;(allow_approximate_merlin)
